$libpath
@echo ********************IMPORTANT*****************************
@echo MAKE SURE THAT THE VULKAN SDK IS INSTALLED BEFORE RUNNING!
set /p libpath=If the vulkan SDK is installed enter it's path:
@echo %libpath%

pause